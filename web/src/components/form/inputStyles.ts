// The single shared input class string for every form primitive in this
// directory -- lifted verbatim from ManualLinkModal (AuditQueuePage.tsx),
// this codebase's only prior form, so a Settings field looks identical to
// the existing manual-link form rather than introducing a second style.
export const INPUT_CLASS =
  "w-full rounded border border-neutral-800 bg-neutral-950 px-3 py-2 text-white focus:outline-none focus:ring-1 focus:ring-brand disabled:cursor-not-allowed disabled:opacity-50";
